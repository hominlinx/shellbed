//fd
/*
 * fd
 */
/*d
 * fd
 */
int bmain() "fda"
{
    //*ds
   return 0;
    return (adgc);
    return new(a) anull;
    RETURN_NEW(new(a) anull);
    return  null;
    return NULL;
    return null;
    return  null;
    return  NULL;
    return NULL== a;
    return null == b;
    return NULL == pSocket ? NULL : pSocket->socketBase();
    bool GetCanvasOffSet(INT iCanvasIndex, INT& iOffSetX, INT& iOffSetY){return false;};
    return;
     BOOL Lock(DWORD msec = INFINITE) {return uc.Lock(msec);}
    return ;
    return false;
    return static_cast<Float32>(fontsize->metrics.max_advance) / 64.0f;
    return SpFont(NEW(Font, face));
    return a>b?a:b;
    return ab;
    return a;
    return m_destWidth;
    return Rotation(-sin, cos);
    return SpName(NEW(NameTypeS, info, dsnD, dsnN));
    //RETURN(spName(NEW(FF))); //error
    RETURN(false);
    //RETURN(temp->send());
    //RETURN(spName());
    return true;
    //RETURN(new(a) a); // error
    RETURN(NULL); // error
}
//return null;











